server:
  port:  8761
eureka:
  client:
    fetch-registry: false
    register-with-eureka:  false #le indicio q no es cliente eureka es el servidor
    service-url:
      default-zone:  http://${eureka.instance.hostname}:${server.port}/eureka/