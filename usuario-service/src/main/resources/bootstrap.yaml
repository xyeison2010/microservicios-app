spring:
  application:
    name:  usuario-service  #me va guardar el nombre de cada microservicio
  cloud:
    config:
      enabled: true
      uri:  http://localhost:8081  #este es el puerto de config server